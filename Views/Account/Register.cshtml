@model Feedo.Models.RegisterViewModel
@{
    ViewData["Title"] = "Créer un compte";
}

<style>
    .auth-container {
        max-width: 500px;
        margin: 3rem auto;
        padding: 2.5rem;
        background: white;
        border-radius: 20px;
        box-shadow: 0 10px 40px rgba(220, 38, 38, 0.1);
    }

    .auth-header {
        text-align: center;
        margin-bottom: 2rem;
    }

    .auth-header h2 {
        color: #dc2626;
        font-weight: bold;
        margin-bottom: 0.5rem;
    }

    .auth-header p {
        color: #6b7280;
    }

    .form-control {
        padding: 0.875rem 1rem;
        border: 2px solid #e5e7eb;
        border-radius: 10px;
        transition: all 0.3s ease;
    }

    .form-control:focus {
        border-color: #dc2626;
        box-shadow: 0 0 0 0.2rem rgba(220, 38, 38, 0.1);
    }

    .form-label {
        font-weight: 600;
        color: #374151;
        margin-bottom: 0.5rem;
    }

    .btn-register {
        background: linear-gradient(135deg, #dc2626 0%, #991b1b 100%);
        color: white;
        padding: 0.875rem 2rem;
        font-weight: 600;
        border: none;
        border-radius: 10px;
        width: 100%;
        transition: all 0.3s ease;
    }

    .btn-register:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 16px rgba(220, 38, 38, 0.3);
        color: white;
    }

    .auth-link {
        text-align: center;
        margin-top: 1.5rem;
        color: #6b7280;
    }

    .auth-link a {
        color: #dc2626;
        font-weight: 600;
        text-decoration: none;
    }

    .auth-link a:hover {
        text-decoration: underline;
    }

    .row {
        margin-left: -0.5rem;
        margin-right: -0.5rem;
    }

    .row > div {
        padding-left: 0.5rem;
        padding-right: 0.5rem;
    }
</style>

<div class="container">
    <div class="auth-container">
        <div class="auth-header">
            <div style="font-size: 3rem; margin-bottom: 1rem;">👤</div>
            <h2>Créer un compte</h2>
            <p>Rejoignez Feedo et commencez votre expérience</p>
        </div>

        <form asp-action="Register" asp-controller="Account" method="post">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

            <div class="row mb-3">
                <div class="col-md-6">
                    <label asp-for="FirstName" class="form-label"></label>
                    <input asp-for="FirstName" class="form-control" placeholder="John" />
                    <span asp-validation-for="FirstName" class="text-danger small"></span>
                </div>
                <div class="col-md-6">
                    <label asp-for="LastName" class="form-label"></label>
                    <input asp-for="LastName" class="form-control" placeholder="Doe" />
                    <span asp-validation-for="LastName" class="text-danger small"></span>
                </div>
            </div>

            <div class="mb-3">
                <label asp-for="Email" class="form-label"></label>
                <input asp-for="Email" class="form-control" placeholder="votre@email.com" />
                <span asp-validation-for="Email" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label asp-for="PhoneNumber" class="form-label"></label>
                <input asp-for="PhoneNumber" class="form-control" placeholder="+33 6 12 34 56 78" />
                <span asp-validation-for="PhoneNumber" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Password" class="form-label"></label>
                <input asp-for="Password" class="form-control" placeholder="••••••••" />
                <span asp-validation-for="Password" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label asp-for="ConfirmPassword" class="form-label"></label>
                <input asp-for="ConfirmPassword" class="form-control" placeholder="••••••••" />
                <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
            </div>

            <div class="mb-4">
                <label class="form-label d-block">Je veux devenir :</label>
                <div class="btn-group w-100" role="group">
                    <input type="radio" class="btn-check" name="Role" id="roleClient" value="Client" autocomplete="off" checked onchange="toggleRole()">
                    <label class="btn btn-outline-danger" for="roleClient">
                        <i class="fas fa-user mb-1 d-block" style="font-size: 1.2rem;"></i>
                        Client
                    </label>

                    <input type="radio" class="btn-check" name="Role" id="roleLivreur" value="Livreur" autocomplete="off" onchange="toggleRole()">
                    <label class="btn btn-outline-danger" for="roleLivreur">
                        <i class="fas fa-motorcycle mb-1 d-block" style="font-size: 1.2rem;"></i>
                        Livreur
                    </label>
                </div>
            </div>

            <div id="livreur-fields" style="display: none; background: #f9fafb; padding: 15px; border-radius: 10px; margin-bottom: 20px;">
                <h6 class="text-danger mb-3"><i class="fas fa-info-circle"></i> Infos Véhicule</h6>
                
                <div class="mb-3">
                    <label asp-for="VehicleType" class="form-label"></label>
                    <select asp-for="VehicleType" class="form-control">
                        <option value="Bike">Vélo 🚲</option>
                        <option value="Scooter">Scooter 🛵</option>
                        <option value="Car">Voiture 🚗</option>
                    </select>
                </div>

                <div class="mb-3">
                    <label asp-for="LicensePlate" class="form-label"></label>
                    <input asp-for="LicensePlate" class="form-control" placeholder="AB-123-CD" />
                </div>
            </div>

            <button type="submit" class="btn btn-register">
                Créer mon compte
            </button>

            <div class="auth-link">
                Vous avez déjà un compte? <a asp-action="Login" asp-controller="Account">Se connecter</a>
            </div>
        </form>

        <script>
            function toggleRole() {
                var isLivreur = document.getElementById('roleLivreur').checked;
                var fields = document.getElementById('livreur-fields');
                if (isLivreur) {
                    fields.style.display = 'block';
                } else {
                    fields.style.display = 'none';
                }
            }
        </script>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
